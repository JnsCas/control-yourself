export enum ExpenseCurrency {
  ARS = 'ARS',
  USD = 'USD',
}

export enum ExpenseType {
  AUTO = 'AUTO',
  MANUAL = 'MANUAL',
}

export enum ExpenseSource {
  GMAIL = 'gmail',
  WEB = 'website',
  TELEGRAM = 'telegram',
}